[package]
name = "ffi-math"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
uniffi = "0.15"
uniffi_macros = "0.15"

[build-dependencies]
uniffi_build = {version = "0.15", features = [ "builtin-bindgen" ]}
